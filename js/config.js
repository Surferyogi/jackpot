/* Jackpot — configuration.
   No backend: everything is stored on the device in localStorage. */
window.JP_CONFIG = {
  APP_VERSION: 'v2026:09:20-05:19', // UTC build stamp — bump on every release together with CACHE_VERSION in sw.js
  STORAGE_KEY: 'jp_profile_v1',
};
